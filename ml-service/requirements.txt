# ─── Thư viện chính cho FastAPI microservice ───────────────────────────────
# Lưu ý: torch chỉ cần khi export model sang ONNX (chạy download_model.py).
# Sau khi đã export, inference chỉ cần onnxruntime — torch không bắt buộc.

# Web framework
fastapi==0.115.5
uvicorn[standard]==0.32.1

# ML / Transformers
transformers==4.46.3
optimum[onnxruntime]==1.23.3
onnxruntime==1.20.1

# Pydantic (validation schema)
pydantic==2.9.2

# Numpy (tính softmax)
numpy==1.26.4

# PyTorch — chỉ dùng khi export ONNX (download_model.py).
# Có thể bỏ khỏi production image nếu chỉ cần inference.
torch==2.5.1
