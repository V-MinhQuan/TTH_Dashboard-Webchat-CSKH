# Shared Python dependencies for the FastAPI backend, Celery worker, and ml-service.

# Backend web/API
fastapi==0.115.5
uvicorn[standard]==0.32.1
pydantic==2.9.2
pydantic-settings>=2,<3
python-dotenv>=1,<2
httpx>=0.27,<1
pytest>=9.0.3,<10

# SQL Server connectivity
pyodbc>=5,<6
pymssql==2.3.0

# Background jobs
celery[redis]>=5.4,<6

# ML service / Transformers
transformers==4.46.3
optimum[onnxruntime]==1.23.3
onnxruntime==1.20.1
numpy==1.26.4

# PyTorch is only needed to export/download the ONNX model via ml-service/download_model.py.
torch==2.5.1
