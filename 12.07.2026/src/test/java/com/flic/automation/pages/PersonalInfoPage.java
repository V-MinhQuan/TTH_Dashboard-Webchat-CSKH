package com.flic.automation.pages;

import org.openqa.selenium.WebDriver;

/** Header personal-information screen; currently shares the profile contract. */
public final class PersonalInfoPage extends SettingsPage {
    public PersonalInfoPage(WebDriver driver) { super(driver); }
}
