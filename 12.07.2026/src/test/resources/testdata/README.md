# Test data

Không lưu credential, token, SQL connection string hoặc dữ liệu người dùng thật tại đây. Fixture tĩnh không nhạy cảm phải ghi rõ TC ID; dữ liệu CRUD runtime dùng prefix `AUTO_<TC_ID>_<timestamp>` và cleanup qua public API/UI an toàn.
